# Generated artifacts
/output/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
.env

# Editors
.vscode/
.idea/
.ipynb_checkpoints/

# OS noise
.DS_Store
Thumbs.db
# Excel lock files
~$*

# Real market data — single source of truth at data/curves/ and data/fixings/.
# The files there are not committed; pull them from your data feed.
data/curves/Curves*.xlsx
data/fixings/*.csv
data/fixings/*.xlsx
data/fixings/*.xls

# Legacy real-data drop location at repo root (still excluded for safety).
/Curves*.xlsx

# Golden-master snapshots are derived from real market data, so do not commit.
# Regenerate locally with: REGENERATE_GOLDEN=1 pytest tests/test_golden_master.py
tests/golden/*.json
