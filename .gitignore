# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
.env

# Editors
.vscode/
.idea/
.ipynb_checkpoints/

# OS noise
.DS_Store
Thumbs.db
# Excel lock files
~$*

# Fixings: still local-only by user's choice
data/fixings/*.csv
data/fixings/*.xlsx
data/fixings/*.xls

# Legacy real-data drop location at repo root
/Curves*.xlsx

# Golden-master snapshots are derived from market data and regenerated locally.
tests/golden/*.json

# Decisions (2026-05-13 PM):
#   data/curves/Curves*.xlsx  -- NOW TRACKED (user request: include real curves in pushes)
#   /output/                  -- NOW TRACKED (user request: include outputs in pushes)
#   data/trades/              -- already tracked
# Fixings remain ignored unless explicitly toggled later.
